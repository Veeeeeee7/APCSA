package Unit3.Lab2;

// write the header for the Petable interface
public interface Petable {
    // this interface will have two methods:
    // a brushHair method which takes no parameters and returns nothing
    // a bite method which takes an Object parameter and returns nothing

    public void brushHair();

    public void bite(Object o);
}